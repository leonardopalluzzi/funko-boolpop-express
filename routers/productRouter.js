const express = require('express')
const router = express.Router()

// Index

router.get('/',)

// Show

router.get('/:slug',)

// Store 

router.post('/',)

// Update 

router.put('/:slug')

// Modify 

router.patch('/:slug')

// Destroy 

router.delete('/:slug')


module.exports = router;